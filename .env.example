# API token for all requests (required)
API_TOKEN=change-me

# Maximum messages to retain per mailbox (oldest are trimmed first)
MAILBOX_MAX_MESSAGES=500

# Maximum bytes to parse for body/attachments; if exceeded, parsing is skipped
MAX_PARSE_BYTES=1000000
